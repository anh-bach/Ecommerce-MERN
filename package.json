{
  "name": "ecommerce-site-iamanh",
  "engines": {
    "node": "v14.16.1",
    "npm": "6.14.12"
  },
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix server && npm start --prefix server && npm install --prefix client && npm run build --prefix client"
  }
}
